spillr
======

R package of miscellaneous functions
- all_equal_mult() compares multiple objects using base R's all.equal()
- glimpse_labels() modifies tibble::glimpse() to show variable labels (if present)